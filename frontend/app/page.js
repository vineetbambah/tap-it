import MobileNav from "./components/mobile-nav";
export default function Home() {

  return (
    <div className="">
      {/* 
        action -> add student + checkin student
        container -> student table + check in table
        mobile nav
      */}
      <div className={``}><MobileNav /></div>
    </div>
  );
}
